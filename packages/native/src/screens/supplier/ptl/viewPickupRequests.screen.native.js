import React from 'react';
import { ScreenWrapperNative } from '../../../components/screenWrapper.native';

export const ViewPickupRequestsScreenNative = ({ navigation }) => {
  return <ScreenWrapperNative navigation={navigation} title='View Pickup Requests' />;
};
